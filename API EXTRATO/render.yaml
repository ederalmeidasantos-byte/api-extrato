services:
  - type: web
    name: api-extrato
    env: node
    plan: free
    buildCommand: cd src && npm install
    startCommand: cd src && npm start
    envVars:
      - key: NODE_ENV
        value: production
