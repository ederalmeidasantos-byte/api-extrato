version: '3.8'

services:
  kentro-api:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - KENTRO_TOKEN=${KENTRO_TOKEN}
      - KENTRO_API_KEY=${KENTRO_API_KEY}
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped



